apiVersion: v1
kind: Service
metadata:
  name: vector
  namespace: canister
  labels:
    app: com.tale.canister.vector
spec:
  selector:
    app: com.tale.canister.vector
  ports:
    - name: ingest
      port: 8687
      targetPort: 8687
